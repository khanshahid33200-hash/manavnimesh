# Project Rules for Manav Nimesh Portfolio Repository

## Git & Deployment Directives
- **NEVER run `git push` to GitHub or deploy to Vercel automatically.**
- Only run `git push` when the user explicitly prompts/requests to push code to GitHub or deploy.
- All local edits, commits, and local testing can be done freely on demand.
